---
import { Image } from 'astro:assets';
import type { Article } from './types/Article';

export type ArticleProps = {
	articles: Article[];
};

const { articles } = Astro.props as ArticleProps;
---

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
	{
		articles.map((article) => (
			<a
				href={`/artykuly/${article.slug}`}
				class="block rounded-lg overflow-hidden shadow-lg transition duration-300 ease-in-out transform hover:-translate-y-1 hover:shadow-xl no-underline text-black dark:text-white dark:bg-gray-800 group">
				<div class="relative pb-56 md:pb-64 lg:pb-72 overflow-hidden">
					<Image
						src={article.data.cover}
						alt={article.data.coverAlt}
						class="absolute inset-0 h-full w-full object-cover transition duration-300 ease-in-out transform group-hover:scale-110"
					/>
				</div>
				<div class="p-6">
					<h4 class="text-2xl font-bold mb-2">{article.data.title}</h4>
					<p class="text-gray-600 dark:text-gray-400 mb-4">
						{article.data.description}
					</p>
					<div class="flex flex-wrap">
						{article.data.tags.map((tag) => (
							<span class="inline-block bg-blue-500 rounded-full px-3 py-1 text-sm font-semibold text-white mr-2 mb-2">
								#{tag}
							</span>
						))}
					</div>
				</div>
			</a>
		))
	}
</div>
