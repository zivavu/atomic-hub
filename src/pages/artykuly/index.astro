---
import { getCollection } from 'astro:content';
import ArticlesFeed from '../../components/ArticlesFeed/ArticlesFeed.svelte';
import RootLayout from '../../layouts/RootLayout.astro';

const articles = await getCollection('articles');
---

<RootLayout title="Atomowe artykuły">
	<main>
		<ArticlesFeed articlesData={articles} client:load />
	</main>
</RootLayout>
